#include<iostream>
#include<cmath>
#include<vector>
#include<iomanip>
#include<algorithm>
using namespace std;
typedef long long int lint;
#define loop(x,a,b) for(int x = a; x < b; x++)
int main()
{
	//your code here
    lint n;
    for(lint i=0;i,n;i++)
    {
        cin>>str;
        if(strcmp(str,"1/2")==0)
            c1++;
        else if(strcmp(str,"1/4")==0)
            c2++;
        else
            c3++;
    }

	return(0);
}
